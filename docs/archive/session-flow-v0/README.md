# Session Flow V0 Archive

These specs are historical reference only.

They were created when session preparation was treated as the next product slice. The product direction has since changed: the MVP should first build the planning system around monthly targets, weekly plans, daily execution, study logging, and weekly reviews.

Do not use these files as current implementation specs. Revisit them later after the planning system exists and session preparation can use weekly-plan context.
